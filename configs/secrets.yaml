# configs/secrets.yaml
# ──────────────────────────────────────────────────────────────────────────────
# Supervisor password for the Orca Kicker web UI (Tab 2 access).
#
# IMPORTANT:
#   - This file must NOT be committed to version control.
#   - Add it to .gitignore:  configs/secrets.yaml
#   - Change the password before deploying to production.
#   - Always edit this file via SSH — it is NOT changeable from the UI.
# ──────────────────────────────────────────────────────────────────────────────

supervisor_password: "vbs"
